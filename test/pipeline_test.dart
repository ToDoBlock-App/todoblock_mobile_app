import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Pipeline initial test', () {
    // A simple test to ensure the pipeline works
    var testString = 'PipelineTest';
    expect(testString, 'PipelineTest');
  });
}
